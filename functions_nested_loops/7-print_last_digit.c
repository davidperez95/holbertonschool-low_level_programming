#include "main.h"
/**
 * print_last_digit - prints the last digit of a number.
 * @n: number to check
 *
 * Return: the last number
 */
int print_last_digit(int n)
{
	int number;

	number = n % 10;

	return (number);
}
