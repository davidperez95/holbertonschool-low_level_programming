#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @x: integer one
 * @y: integer two
 *
 * Return: the sum of x and y
 */
int add(int x, int y)
{
	int add = 0;

	add = x + y;

	return (add);
}
